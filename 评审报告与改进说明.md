# 评审报告与改进说明

> 评审对象：GitHub 仓库 `Frederic-123-cell/numodel-2026`（CUMCM2026 B 题）。
> 评审时间：2026-09-12。评审方式：逐文件阅读 + 用脚本复算 12 局客户端日志 + 与模拟器官方 `result.json` 对账 + 运行 `q1_region_example.py` 复现问题 1 算例。
> 本副本在原仓库基础上做了**可复现性优先**的改进：`robot_client.py` 默认参数保持不变（不改变已提交正式结果），新增能力均以可选开关提供。

---

## 一、总体结论

**建模思路、代码实现、论文与日志的一致性整体质量较高，核心结论基本成立。** 论文表 1、表 2 中的每一个统计量（清除数、平均交会残差、平均定位清除时间、虚拟时间）都能在对应客户端日志中复算得到，误差在四舍五入范围内完全吻合，这一点在竞赛论文中很难得。

但存在 4 个需要正视的问题（1 个属于提交硬伤、1 个属于源数不可验证、2 个属于表述/口径问题），已在副本中修正或标注。

---

## 二、已核验正确的部分

| 核验项 | 结论 |
| --- | --- |
| 问题 1 算例 | 运行 `q1_region_example.py` 输出与论文 §4.5 **逐位一致**：D(R)=23.74 m、r\*=11.99 m、Jung 上界 13.71 m、"直径圆不能覆盖"。误差带/半平面/凸包/最小包围圆算法正确。 |
| 问题 2 公式 | Δ≈d·tanε/sinφ 推导与"φ→90° 最优、φ<30° 不可靠"结论合理；Thales 布站准则正确。 |
| 问题 3（表 1） | 演练-2/3、正式-1/2/3 的 spread 均值/最大值、定位清除时间均值/最大值、虚拟时间**全部与日志吻合**；正式 3 局共 42 次清除尝试、1 次未中后自愈，与论文一致。 |
| 问题 4（表 2） | 6 局全部吻合；正式 46 次尝试、9 次未中全部自愈、重复清除抑制触发 34 次，与论文一致；演练-1/2 确为 800 m 误配置、演练-3 确为 500 m+v2.1，论文对此的说明属实。 |
| 协议合规 | 串行发送、每动作新 `request_id`、断网复用原请求、同时检查 HTTP 与 `accepted`、`try/finally` 必发 `/exit`、请求/响应/决策全落盘，符合附件 2 §12。 |
| 问题 4 定量论证 | 800 m 网格扇区内约 2~3 点、500 m 约 6~7 点、12 点环绕必命中 180° 扇区，推导正确。 |
| 目录/日志溯源 | 演练局源数与官方 `practice-*.result.json` 的 `jammer_count` 完全对应（P3: 12/15/11，P4: 16/16/12）。 |

---

## 三、发现的问题

### （一）高优先级

1. **正式局源数不可验证，"100% 清除"是推断而非实测。**
   正式局官方 `formal-*.result.json` 为 `version:1`，**不含 `jammer_count`**。论文的 14/15/12、16/10/11 均按"零示向度频道 = 空频道"推断。若某局存在一个**全程零示向度**的源，就会被漏计，清除率将小于 100%。
   该假设有强支撑（覆盖密度 + 6 局演练官方验证），但论文摘要与结论把它写成既成事实，风险偏高。→ **已在论文摘要、6.5、附录 C 增加明确限定**。

2. **"平均定位偏差"指标名不正确。** 论文表格中的数字实际是 **spread（各示向线到估计点的最大垂距）**，是交会几何的**自洽性/残差**，**不是估计点到真实源的距离**。模拟器不返回真实坐标，无法计算真实定位误差。
   → **已全部改名"平均交会残差(spread)"并加注说明**。

3. **P3 演练-1（12 源）的指标不可直接复核。** 其日志 `03_代码/robot_log.jsonl` 由**早期代码版本**生成，**无 `run_start`/`summary`/`strat` 事件**。可验证：12 次 `clear success`、末态 vt=10098.2（与论文一致）；**无法**从该文件复核论文的"2.85 m / 1543 s / 最大 7.6 / 5299"。
   → 建议：提供指标复算脚本，或在该行注明数据来源已不可追溯。**（本次未改数，仅提示）**

4. **论文与 AI 材料未按官方格式提交。** 目前论文只有 Markdown，缺《论文格式规范》要求的封面/承诺书/编号页，也未导出 PDF；`AI工具使用详情.md` 也未按要求导出为 `AI工具使用详情.pdf`。这是**提交层面的硬伤**，必须在 2026-09-13 17:30 前处理。
   → 已在 `说明.md` 的"提交前检查"中列为必做项。

5. **`03_代码` 与 `04_模拟器日志/客户端日志` 中的 15 个日志完全重复**，且 `03_代码` 里还散落系统测试用的短日志（121736/121746/123515）。支撑材料冗余且易混淆。
   → 建议只保留 `04_模拟器日志` 一份，并统一说明 3 个短日志的用途。

### （二）中优先级

6. **问题 2 §5.3 表格数值小误差**：φ=10° 时 Δ=1000·tan1°/sin10°=**100.5**（论文写 100.2）；φ=20° 为 **51.0**（论文写 51.1）。→ **已修正**。

7. **"参数全部先验确定"表述过强。** 论文 7.6 自述 v2.1 抑制阈值与环绕补探参数来自**实局病态诊断**，与 6.6.5/8.1 的"全部先验"互相矛盾。→ **已改为"核心参数先验确定"并在摘要/8.1 注明 v2.1 来源**。

8. **论文 8.4 建议的"观测方位跨度 ≥120°"判据对 7.6.1 的病例无效**：病例最大方位空隙 200°（覆盖跨度仅 160°），160°>120° 不会被拒。真正应拦截的是**最大空隙**。
   → 已在代码中实现可选 `MAX_ANGLE_GAP`（最大方位空隙阈值）作为更合理的判据。

9. **`说明.md` 进度表过期**（仍写"问题4 待演练验证""论文其余章节待做"）。→ **已更新**。

### （三）低优先级 / 鲁棒性

10. 原 `_post` 把请求日志写在**收到响应之后**，进程崩溃或响应丢失时该请求不在日志中。→ **已改为发送前落盘**。
11. 时间预算检查只在网格点级；测试被中断后仍会对该点剩余频道继续发请求。→ **已在频道循环加 `time_up()` 守卫**（扫描/验证/收尾三处）。
12. 环绕补探与收尾清除未纳入 v2.1 重复清除抑制，理论上仍可能重复无效清除（实测未发生）。
13. 正式局存在 `formal_index` 与时间上的轻微疑问：p3-2 与 p3-3 结束仅隔 19 s，p4-2/p4-3 亦然——与"三次独立正式测试"不矛盾（虚拟时间推进很快），但建议在论文中说明各局的真实执行起止，避免评委误判为重复测试。

---

## 四、本副本已实施的改动

**代码 `03_代码/robot_client.py`（默认行为不变，不影响正式结果复现）**
- 请求日志改为**发送前**落盘；
- 主扫描、验证轮、收尾循环增加**频道级时间守卫**；
- 新增 `angular_gap()` 与常量 `MAX_ANGLE_GAP`（默认 `360` = 关闭）：设为小于 360 时启用**单侧几何拦截**，即论文 8.4 改进方向 1 的正确实现；
- 通过 `py_compile` 语法检查。

**论文 `02_论文/B题论文_正式稿.md`**
- "平均定位偏差"→"平均交会残差(spread)"，并在摘要、指标口径、附录 C 注明其非真实定位误差；
- 修正问题 2 数值表；
- 摘要/6.5/8.1 增加正式局源数推断的限定；
- "全部先验确定"改为"核心参数先验确定"。

**`说明.md`**：更新进度表；新增"提交前检查"清单。

**新增**：本报告。

---

## 五、提交前必须人工完成的事项

1. 按官方格式排版导出 **论文 PDF**（含承诺书/编号页），核对页数/匿名要求；
2. 导出 **`AI工具使用详情.pdf`**，补齐三名队员签名与日期；
3. 从模拟器**原样导出**正式日志（**不要改文件名**），确认 `.jlog` ≤2 MB 且已进入上传队列；
4. 统一支撑材料目录，去重日志；
5. 如时间允许：对 P3 演练-1 补一份指标复算说明；用 `MAX_ANGLE_GAP`（如 210°）重跑问题 4 演练，验证单侧拦截不伤清除率后再考虑纳入。

---

## 六、结论

模型正确、算法可行、结果可复现，**已具备提交条件**；主要风险不在技术，而在**正式局源数的不可验证性**与**提交格式**。按第五节处理后可提交。
