# 数模赛内容 · 文件夹说明

**选题：CUMCM2026 B 题 —— 无线电干扰源的快速自动定位与清除**

---

## 目录结构

```
数模赛内容/
├── 01_题目原文/                 题目与附件原文
│   ├── A题.txt ~ E题.txt        五道题完整文本（方便对比/选题复盘）
│   ├── B题_附件1_模拟器使用说明.txt   机器狗行为规则、计时、截止时间
│   ├── B题_附件2_通信协议.txt        4 条 HTTP 指令的完整 JSON 协议
│   └── CUMCM2026Problems.zip    官方原始题目包（含全部 xlsx/docx 附件）
├── 02_论文/
│   ├── B题论文_正式稿.md         ⭐ 全组兜底正式稿（摘要/问题分析/四问建模/12局结果/灵敏度/参考文献/附录齐全）
│   ├── AI工具使用详情.md         ⭐ 按组委会2026规定撰写的支撑材料（提交时导出PDF：AI工具使用详情.pdf）
│   ├── B题论文草稿.md            工作草稿（保留调优过程原始记录）
│   └── B题论文草稿_备份_20260911.md  正式稿动笔前的草稿备份
├── 03_代码/
│   └── robot_client.py          机器狗客户端（v2 密网格策略 + 按次独立完整日志）
└── 04_模拟器日志/                ✅ 12 局全部日志已归档（客户端逐请求日志 + 模拟器官方记录），见 README.md
```

## 进度

| 部分 | 状态 |
|---|---|
| 问题 1 建模（交会定位区域直径 + 圆覆盖判定） | ✅ 已完成 |
| 问题 2 建模（误差传播 + 第二检测点选择 + 候选区域） | ✅ 已完成 |
| 问题 3 建模与仿真（全向源搜索清除策略） | 🟢 **全部完成**：演练 3 局全清 + 正式 3 局全清（14 / 15 / 12 源，42 次尝试仅 1 次未中后自愈） |
| 问题 4 建模与仿真（加入定向源） | 🟢 **全部完成**：演练 3 局全清 + 正式 3 局全清（16 / 10 / 11 源，46 次尝试 9 次未中全部自愈，34 次 clear_skip 抑制） |
| L1 客户端骨架（四项指令/重试/日志） | ✅ 已完成 |
| 问题3 策略 v2（密网格 800m + 3 示向度交会 + 20m 清除） | ✅ 演练验证通过（偏差均值 2.85m，正式 42 次尝试 1 次未中自愈） |
| 问题4 策略（网格 500m + 定向源环绕补探 12 点 + v2.1 抑制） | ✅ 演练 3 局 + 正式 3 局全部全清（46 次尝试 9 次未中全部自愈） |
| 论文正式稿、灵敏度分析、附录、AI 工具使用详情 | ✅ 已完成（Markdown 稿，提交前需按官方格式排版导出 PDF） |

## 怎么跑客户端

> `ROBOT_ID` 已填好 `202606010578`（你们队号）。若换账号登录，必须同步改这个值。

1. 打开官方模拟器 → 登录 → 进入「问题3 演练测试」→ 点开始 → 等 5 秒倒计时结束、界面显示「机器狗接口已就绪」。
2. 命令行进入代码目录并运行：

```bash
cd 数模赛内容\03_代码
python robot_client.py            # 连通性冒烟测试（先确认接口通）
python robot_client.py strategy   # 跑问题3 搜索-定位-清除策略（全向源，网格800m）
python robot_client.py strategy4  # 跑问题4 策略（混入定向源：网格500m + 环绕补探）
```

- `strategy` / `strategy4` 模式会**自动等接口开放再进**场，所以也可以先启动程序、再点开始，顺序无所谓。
- 问题 4 演练请对应点「问题4 演练测试」按钮再运行 `strategy4`。
- 每次运行在 `03_代码/` 下生成**独立**日志 `robot_log_<时间戳>.jsonl`（含全部请求/响应 + 每次定位清除决策，异常分支也显式记录），可据此填论文表 1/表 2 并作附录。
- 程序结束会打印 `已清除频道 / 未被清除频道 / 末态虚拟时刻`，**把这段终端输出截图给我**。

## 关键约束（务必记住）

- ⏰ 北京时间 **2026-09-13 17:30 后不能启动新测试**，建议 **15:30 前** 跑完所有正式测试。
- 模拟器需联网登录，**同一账号不能在两台设备同时使用**。
- 只能**串行**发指令，不能并发；断网重试必须复用原 `request_id` 与请求体。
- 加密行为日志 **≤ 2MB**，别写异常高频循环。
- 问题 3 / 问题 4 正式测试**各只有 3 次机会**，演练测试不限次数——先在演练里调好策略。

## 提交前检查（本副本已完成的改进见《评审报告与改进说明.md》）

1. 论文 `.md` 需按《全国大学生数学建模竞赛论文格式规范》排版并导出 **PDF**（含承诺书/编号页），正式提交以 PDF 为准；
2. `AI工具使用详情.md` 需导出为固定文件名 **`AI工具使用详情.pdf`**，并补齐三名队员签名与日期；
3. 正式测试日志须从模拟器**原样导出、勿改文件名**，与客户端日志一并作为支撑材料；确认加密日志 `.jlog` 单文件 ≤ 2MB、已进入上传队列；
4. 核对论文中 12 局数字与 `04_模拟器日志` 逐条一致（已核对，见评审报告）；
5. `robot_client.py` 冻结版默认参数保持不变（`MAX_ANGLE_GAP=360`），确保正式结果可复现；如需启用论文 8.4 的单侧几何拦截，再单独演练验证。
